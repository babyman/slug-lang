var {*} = import(
    "slug.std",
    "slug.test",
)

@test
var testForeignFunctionErrorsAreRuntime = fn() {
    var r = runSafe(fn() { import("foo.bar") })
    r.error.type /> assertEqual("error")
}
