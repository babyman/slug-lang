
@export
foreign hasTag = fn(v, @str tag);

@export
foreign getTag = fn(v, @str tag);

@export
foreign searchModuleTags = fn(@str module, @str tag, @bool includePrivate = false);

@export
foreign searchScopeTags = fn(@str tag);
