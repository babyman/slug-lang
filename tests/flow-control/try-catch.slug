var {*} = import(
	"slug.std",
	"slug.test",
);

//
// throwing a catching errors
// --------------------------

var f1 = fn() {
	defer onerror(err) {
		"new return value"
	}
	println("  before throw")
	throw "Failed to connect";
	println("  after throw")
}

println("before call");

f1() /> assertEqual("new return value");

println("after call");



