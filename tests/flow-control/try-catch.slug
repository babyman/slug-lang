
//
// throwing a catching errors
// --------------------------


var f = fn() {
    try {
        println("hello slug!")
        // throw FileError({ path: "/path/to/file" });
        throw {"type": "ConnectionError"}
    } catch err {
        {"type": "ConnectionError"} => println("Connection failed");
        {"type": "FileError"} => println("File error");
        _ => {
        //         // todo needs to be in a block, update the parser
                // throw err;
        //         println("ddddddd");
            }
    }
}

f();
println("next");
