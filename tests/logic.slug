import slug.test.*;

// logic checks

// and
// --------
assert(true && true, "true && true");
assert(true && false == false, "true && false == false");
assert(!(true && false), "true && false");

// or
// --------
assert(true || true, "true || true");
assert(true || false, "true || false");

